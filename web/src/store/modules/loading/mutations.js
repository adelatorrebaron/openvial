

const SHOW_LOADING = (state, status) => {
      state.loading = status
}

const HIDE_LOADING = (state, status) => {
      state.loading = status
}


export default {
      SHOW_LOADING,
      HIDE_LOADING
}